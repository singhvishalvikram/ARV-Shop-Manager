org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8
android.useAndroidX=true
android.nonTransitiveRClass=true
# Signing config comes from ~/.gradle/gradle.properties or env — NEVER commit
# keystore paths/passwords here (GUARDRAILS 1.4).
