<?xml version="1.0" encoding="utf-8"?>
<!-- Prod is HTTPS-only. Cleartext is permitted ONLY for the emulator loopback
     (10.0.2.2) in dev builds, so the app can reach a local FastAPI during
     development without weakening production transport security (GUARDRAILS 1.3). -->
<network-security-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
    </domain-config>
    <base-config cleartextTrafficPermitted="false" />
</network-security-config>
