org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8
android.useAndroidX=true
android.nonTransitiveRClass=true
# Signing config is supplied via ~/.gradle/gradle.properties or environment —
# NEVER put keystore paths or passwords in this committed file (see CLAUDE.md).
